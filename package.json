{
  "name": "grok-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin for using the local Grok CLI.",
  "type": "module",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "claude plugin validate --strict ."
  }
}

